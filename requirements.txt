# Core scientific stack
numpy
pandas
scipy
scikit-learn
matplotlib
plotly

# Statistics — statistical parametric mapping of 1D biomechanical waveforms
# (optional; only needed for bioscout.utils.stats.spm_* functions)
spm1d

# Data / file I/O
pyyaml
c3d
packaging
pyperclip

# Load tracking — fitness-tracker imports (.fit needs fitparse; .tcx/.gpx/.csv use stdlib)
fitparse
requests   # Zepp/Huami + Strava cloud pulls

# GUI
customtkinter
Pillow
psutil
pyautogui
pygetwindow
screeninfo

# Video / recording (optional — needed for recording tab)
opencv-python
mediapipe

# NOTE: opensim must be installed separately — it is not on PyPI.
# Install via conda:  conda install -c opensim-org opensim
# or download the Python bindings from https://simtk.org/projects/opensim
